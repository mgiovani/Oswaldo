
    create table Curso (
        id integer not null auto_increment,
        descricao varchar(255),
        duracao integer,
        nome varchar(255),
        qtdHoras integer,
        primary key (id)
    );
